schema{
 query:Query
}

type Query {
	provincias: [Provincia]
  	provincia(id: Int): Provincia
}

type Provincia {
  	id: Int
  	nombre: String
  	region: Region
}

enum Region {
	NOROESTE
	NORDESTE
	PAMPEANA
	CUYO
	PATAGONIA   
}